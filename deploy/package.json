{
    "name": "deploy",
    "version": "0.0.1",
    "description": "",
    "main": "",
    "scripts": {
        "storage": "echo \"START Formatting storage...\" && tsc initStorage.ts --resolveJsonModule --esModuleInterop && node initStorage.js > storage.json && echo \"Storage updated in storage.json END\""
    },
    "author": "Smart-Chain <contact@smart-chain.fr>",
    "license": "MIT",
    "dependencies": {
        "@taquito/michelson-encoder": "^14.0.0",
        "@taquito/signer": "^14.0.0",
        "@taquito/taquito": "^14.0.0",
        "dotenv": "^16.0.2",
        "fs-extra": "^10.1.0"
    },
    "devDependencies": {
        "typescript": "^4.8.4"
    }
}
