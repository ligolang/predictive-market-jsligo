const not_manager : string = "Not the Manager of the contract";
const not_manager_nor_signer : string = "Not the Manager or signer of the contract";

const same_previous_manager : string = "New Manager address can't be the same as the current one";
const same_previous_signer : string = "New Signer address can't be the same as the current one";

const no_event_id : string = "No Event with this ID";
const event_creation_paused : string = "Event Creation is Paused";
